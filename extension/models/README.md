# models

ONNX model files for the local vision detector and any local OCR/NER models land here
(TODO(stage-5)/(stage-6)/(stage-7)). Not committed to git if large — see repository conventions
once real weights are added. Models are bundled or cached locally; never fetched as remote code at
runtime (AGENTS.md invariant 7).
