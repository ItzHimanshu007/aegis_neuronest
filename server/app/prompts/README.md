# prompts

TODO(stage-3): the static prompt prefix used to keep the vLLM KV cache warm across requests, and
the JSON-schema-constrained output configuration for plan.v1. See docs/architecture.md layer 6.
